name: Test GitHub Actions
on: push
jobs:
  test-expression:
    name: Test GitHub Actions expressions
    runs-on: ubuntu-latest
    steps:
      - run: "echo expression value: $EXPRESSION"
        env:
          EXPRESSION: ${{ contains([Son, Nguyen], son) }}
